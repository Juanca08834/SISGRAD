package com.unicauca.auth.domain.model;

/**
 * Enum que representa los roles de usuario en el sistema
 */
public enum Rol {
    DOCENTE,
    ESTUDIANTE,
    COORDINADOR,
    JEFE_DEPARTAMENTO
}